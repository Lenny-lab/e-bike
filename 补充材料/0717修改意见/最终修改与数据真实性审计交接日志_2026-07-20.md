# 小电驴项目最终修改与数据真实性审计交接日志

> 记录起点：2026-07-20 00:30（北京时间）  
> 本次写入：2026-07-20 11:40:46（北京时间）  
> 项目根目录：`G:\我的云端硬盘\数据新闻大赛协作\小电驴`  
> Git 分支：`main`  
> GitHub：`https://github.com/Lenny-lab/e-bike.git`

## 1. 当前状态（必须先读）

本轮工作尚未最终提交、尚未推送。最近一次已经确认推送成功的提交是 `ba91d6b`。该提交之后，本地工作区又修改了以下文件：

- `index.html`
- `css/style.css`
- `js/style-C.js`
- `js/vendor/data.js`

因此，下一个 agent 不能把 `ba91d6b` 当作最终版本，也不能在未检查差异前覆盖这四个文件。当前应先运行：

```powershell
git status --short
git diff --check
git diff -- index.html css/style.css js/style-C.js js/vendor/data.js js/vendor/main.js
```

注意：云盘路径的 Windows 沙箱频繁出现 `windows sandbox: helper_unknown_error: setup refresh had errors`；用户已经明确授权：仅对重要任务，在沙箱失败时可在沙箱外执行。审批通道还偶发 `stream disconnected`，遇到时不能把未返回结果的命令当作已执行。

## 2. 已确认的核心数据来源与复算结果

### 2.1 2025 年海关原始表

原始文件：

`G:\我的云端硬盘\数据新闻大赛协作\小电驴\补充材料\0718补充材料\2025年出口数据-海关总署.xlsx`

商品编码：`HS 87116000`。

已经从原始 Excel 重新汇总并落盘到：

- `数据汇总_2025海关出口.json`
- `数据真实性审计报告_2026-07-20.md`

复算结果：

- 2025 出口数量：`26,726,934 辆`，页面可写 `2,672.69 万辆`，整数展示可写 `2,673 万辆`。
- 2025 出口金额：`6,826,606,215 美元`，页面可写 `68.2661 亿美元`，两位小数展示为 `68.27 亿美元`。
- 原始表中数量或金额为正的贸易伙伴记录：`199 个`。应写“199 个有正值记录的贸易伙伴”，不要写“180 多个国家”。
- 2025 出口额 TOP12 占比：`61.9681%`，页面写 `62.0%`。
- 2025 出口量 TOP12 占比：`66.2014%`。

### 2.2 统一时间序列

网页统一使用：

- 2020—2024：UN Comtrade HS 8711.60.90。
- 2025：上述海关总署 HS 87116000 国别明细表。
- 2026Q1：单独展示，不进入 CAGR、不进入 2020—2025 累计；来源为厦门广电 / 光明日报 2026-06-04 援引行业通报，必须明确标注为媒体援引，不得冒充海关原始表。

2020→2025（五个年度间隔）重新计算：

- 数量 CAGR：`8.30%`。
- 金额 CAGR：`14.26%`。
- 数量累计增幅：`49.0%`。
- 金额累计增幅：`94.8%`。

旧的 2018 基期 `1195 万辆 / 25.34 亿美元`、`12.2% / 15.2%` 七年 CAGR 因口径未与 2020+ 序列对齐，必须从可执行数据和页面中删除，不能只在页面上隐藏。

## 3. 本轮已经落盘的修改

### 3.1 部件弹窗“内容一字一行、看不完整”

问题位置：

- `js/style-C.js` 中 `showPartCard(item)`，约 1360—1395 行。
- `css/style.css` 中 `.cd-row` / `.part-detail-body`，约 2730—2805 行。

根因：弹窗正文复用了 `.cd-row` 的两列网格 `32px 1fr`，但 HTML 没有图标列，正文被挤入 32px 窄列，出现一个汉字一行。

已做修改：

- 将部件弹窗正文改成独立的 `.part-detail-body` 与 `.part-detail-copy`。
- 正文改为完整宽度、正常换行、左对齐、首行缩进两格。
- 增加移动端弹窗 padding 规则。

注意：之前的字符串替换同时命中了 `js/style-C.js` 中另一处共用弹窗正文。结构本身不会再窄列断字，但下一个 agent 必须检查那一处是否是国家弹窗；若属于国家档案，应把标签“结构说明”改为“市场说明”，不要误写为部件说明。

### 3.2 分解图下半部分被裁切

问题位置：

- `index.html` 的 `#explodedScooter`，约 1165—1210 行。
- `css/style.css` 的 `.exploded-stage` / `.exploded-svg`，约 1772、5093、6595 行。

根因：后置 CSS 把 `.exploded-stage` 设置为 `overflow:hidden`；点击“分解”后，部件通过 GSAP 移出原始 `viewBox="0 0 1100 580"`，因此左侧车架、下方电池/链条、上方鞍座等被裁掉。

已做修改：

- SVG `viewBox` 扩大为 `-80 -55 1260 690`。
- 添加 `preserveAspectRatio="xMidYMid meet"`。
- `.exploded-stage` 改为 `overflow:visible; aspect-ratio:auto`。
- `.exploded-svg` 改为 `height:auto; max-height:none; overflow:visible`。

尚未完成：必须在真实浏览器里点击“分解”，检查 8 个部件、8 条引线、8 组标签都完整显示；再点击至少 3 个部件检查弹窗。当前因浏览器验证技能文件被沙箱故障阻断，尚未完成真实渲染截图，不能在最终报告中谎称“视觉验收通过”。

### 3.3 正文排版

已落盘：

- 开篇 hero 中 `<em class="dropcap">从</em>` 已替换为完整的 `<span class="lede-opening">从中国港口启程</span>`，不再只做“从”字下沉。
- `.prose p` 和 `.prose p + p` 从 `text-indent:0` 改为 `text-indent:2em`，正文左对齐。
- `r17-prose p` 的缩进也尝试统一为 `2em`。

待检查：CSS 后部可能仍有更高优先级覆盖，必须在浏览器中检查正文是否全部左对齐、首行缩进两格，标题仍居中。

### 3.4 2025 数据与旧口径清理

已落盘：

- 首页第四个核心指标从“180+ 个国家和地区”改为“2025 年 199 个有正值记录的贸易伙伴”。
- 2025 印尼、美国、日本国家卡数据更新为海关原始表口径。
- 图 1 改为 2020—2025 统一序列；旧 2018 基期卡删除。
- CAGR 卡改为 2020 基期、2025 末值、五年 CAGR 8.30% / 14.26%。
- 2024 区域占比卡因区域重复归类且未按 2025 全量数据复算，已从 `index.html` 撤下。
- `js/style-C.js` 中旧 insight 数字 `2210 / 58.6 / 24.8 / 180` 已清理或替换。
- `js/vendor/data.js` 中 `market.yearly` 已删除 2018—2019，只保留 2020—2025。
- `chinaProduction` 的混合口径估算数组已计划置空。
- 风险流程中的“认证 6–12 个月”和“待核实原文”已撤下，官方法规来源改为 EUR-Lex 2025/114、2025/120。

## 4. 当前仍存在的明确问题（下一位必须先修）

### 4.1 `js/vendor/data.js` 有字面换行错误

最近一次实际读取确认，约第 140 行存在：

```js
// 混合了规模以上与全行业估算的旧产量序列已撤下`r`n    chinaProduction: [],
```

这里的 `` `r`n `` 被当成普通字符写进注释，导致 `chinaProduction: []` 也落在注释中。JavaScript 语法仍可能通过，但对象字段没有按预期存在。

必须改成两行：

```js
// 混合了规模以上与全行业口径的旧产量序列已撤下
chinaProduction: [],
```

### 4.2 文件头仍残留“B 类估算，需后续补强”旧说明

`js/vendor/data.js` 约 14—16 行仍有：

```text
风险：……保留为行业示意 (B 类估算)，需后续补强。
```

必须删除，替换为：

```text
数据使用原则：无本地原始表或可追溯公开来源支撑的数值不进入页面。
```

### 4.3 “业界观察 · 非权威数据”仍可能显示

位置：

- `js/vendor/data.js` 的 `valueChain.regionMarketsNote` 与 `valueChain.markets`，约 249—300 行。
- `js/vendor/main.js` 中 `marketCards` / `marketSection`，约 180—220 行。

最近一次尝试删除该模块的 PowerShell 命令因引号解析错误失败，确认没有完成。当前页面仍可能渲染：

- “业界观察 · 非权威数据”
- “方向性判断”
- 未经同口径海关表复核的区域原因卡

必须执行：

1. 将 `valueChain.markets` 改为 `[]`。
2. 将 `regionMarketsNote` 改为空字符串。
3. `main.js` 中用 `const verifiedMarkets = Array.isArray(vc.markets) ? vc.markets : [];`。
4. `marketCards` 为空时 `marketSection` 必须返回空字符串，不生成任何标题或空框。

### 4.4 旧注释和缓存版本需复查

需要扫描：

```powershell
rg -n -i "示意 B|非官方汇总|模拟数据|暂定|待核验|待复核|待与 UN|待补充|待核实|1195|25\.34|12\.2%|15\.2%|58\.6|24\.8|2210|业界观察|非权威数据|estimated|all-approx" index.html js css
```

最近一次扫描只确认到：

- `js/vendor/data.js` 仍有上述估算说明与字面换行问题。
- `js/vendor/main.js` 仍有“业界观察 · 非权威数据”。
- `css/style.css` 有一条“替代无来源认证成本模拟图”的代码注释。该注释不展示，但为了审计整洁可改为“官方法规路径”。

静态资源缓存版本的更新命令曾遇到审批通道断流，是否落盘未确认。应检查 `index.html` 顶部 CSS 链接和底部 `data.js / style-C.js` 的 `?v=`，统一提升到新版本，例如 `v=46`，避免线上继续使用旧缓存。

## 5. 数据真实性分级与结论

### 5.1 可以作为核心结论的数据

- 2025 海关总署 Excel 直接汇总：数量、金额、199 个贸易伙伴、国别排序、TOP12 占比。
- 2020—2024 UN Comtrade 同一 HS 序列。
- 2025 公司年报 / 交易所公告中的品牌经营数据，但必须保留公司口径说明，不能把含滑板车的销量直接当道路两轮电动车销量。
- EUR-Lex 法规编号与税率。
- 英国 OPSS 官方报告中的 2024 火灾数据，必须注明只适用于英国，不能外推全球。

### 5.2 可以保留但必须醒目标注来源层级的数据

- 2026Q1 720 万辆、同比 68.2%：媒体援引行业通报，不是本地海关原始表；必须单列。
- 地方政府、行业协会、公开报道中的锡山、丰县、天津等数据：可以引用，但不能跨口径做严格排名。
- 受访者数字：必须明确写“受访者称 / 访谈口径”，不要冒充全国统计。

### 5.3 必须撤下的数据

- 无原始表支撑的配套率、国产化率、认证成本、认证时长。
- “示意 B”“非官方汇总”“模拟”“暂定”“待核验”等表述及其背后的数字。
- 2018—2019 未与 2020+ 对齐的旧基期和由此计算的七年 CAGR。
- “业界观察 · 非权威数据”区域卡。
- 无明确年报或公开来源支撑的品牌宣传型精确数字（例如市场份额、海外营收占比、精确产能、精确售价）。

当前结论：核心 2025 海关数据已经真实复算；但在完成第 4 节的三个代码清理、真实浏览器验证和最终全文扫描之前，不能宣称“网页已完全不存在模拟数据”。

## 6. 历史修改清单验收摘要

以下项目已经在代码中找到并基本完成，但最终仍需浏览器逐项验收：

- 主标题改为“一辆中国小电驴，何以驶向海外大世界？解码中国电动车出海十年”。
- `深度数据·DATA STORY`，删除参赛作品后缀。
- 顶部英文改为 `Data Report: China’s E-Bicycle Industry`。
- 左上中文改为“骑着电驴游世界”，统一黑色。
- 删除错误“联系我们”入口。
- 删除第二行五个章节跳转导航。
- 删除目录页“本作品·共 5 章·96 页·三组协作”。
- 身份卡引导语拆行、缩小“点击卡片翻面查看”。
- 删除第一、第二张翻面卡中的天津数据。
- 倒车语音逻辑：点击播放当前语言，播放结束后再切换下一语言。
- “队长”改“主编”，指导老师增加朱威。
- 主编改为卜言庆，删除“数据新闻大赛参赛作品”。
- 时间线末项改为 2024，只保留 GB 17761-2024 发布信息。
- 欧洲场景删除“具体年度数据需 Eurostat 子项核验”。
- 下一期文案改为厕所革命第 2 期。
- 页脚改为“骑着电驴游世界编辑部”，右下方旧文字删除。
- 3.4 标题单行显示。
- 电池技术图改成三个阶段卡片和箭头。
- 部件图不再展示无原始表支撑的配套率或价格。

## 7. 下一位 agent 的推荐执行顺序

1. 只修第 4.1、4.2、4.3 三个明确问题，不要先改其他内容。
2. 运行：

```powershell
node --check js/vendor/data.js
node --check js/vendor/main.js
node --check js/style-C.js
git diff --check
```

3. 运行第 4.4 的全文扫描；所有命中逐条判断，页面可见的模拟/待核验内容必须为 0。
4. 启动本地网页，实际点击：
   - “分解”按钮；
   - 8 个部件；
   - 至少 3 个弹窗；
   - 关闭弹窗；
   - 移动端宽度下横向滚动。
5. 截图确认：左侧车架、顶部鞍座、右侧电机、底部电池盒与链条、全部标签均未裁切；弹窗正文不是一字一行。
6. 检查所有正文：左对齐、首行缩进 2em，标题居中，标点无乱码。
7. 更新根目录 `数据真实性审计报告_2026-07-20.md` 和 `执行日志_2026-07-20起.md`，把本文件作为详细交接附件。
8. 提交前运行 `git diff --check`，确认无意外空行、无乱码、无 desktop.ini。
9. 建议提交信息：

```text
fix: complete visual clipping repair and final data authenticity audit
```

10. 推送：

```powershell
git add index.html css/style.css js/style-C.js js/vendor/data.js js/vendor/main.js 数据真实性审计报告_2026-07-20.md 执行日志_2026-07-20起.md 补充材料/0717修改意见/最终修改与数据真实性审计交接日志_2026-07-20.md
git commit -m "fix: complete visual clipping repair and final data authenticity audit"
git -c http.version=HTTP/1.1 push origin main
```

如 `git push` 再报 curl 55 / HTTP2 错误，优先使用上面的 HTTP/1.1 命令，不要改写历史。不得使用 `git reset --hard` 或覆盖用户已有改动。

## 8. 本轮验证证据

已确认：

- `node --check js/vendor/data.js` 在最近一次成功执行时通过。
- `node --check js/style-C.js` 在部件弹窗修改后曾通过；其后没有成功写入新的 style-C 修改。
- 最近一次 Git 状态确认四个文件处于修改状态：`css/style.css`、`index.html`、`js/style-C.js`、`js/vendor/data.js`。
- 最近一次明确扫描确认旧 2018 基期、12.2% / 15.2%、“待核验”等主要字符串已基本清除；随后又发现并记录第 4 节残留。

未确认：

- `js/vendor/main.js` 尚未修改成功。
- 浏览器实际点击与截图验收尚未完成。
- 本轮修改尚未提交、尚未推送。

因此，接手者必须以“待完成最终三项清理 + 视觉验收 + 提交推送”的状态继续，不能从头重做，也不能直接宣布完成。